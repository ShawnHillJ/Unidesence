#Mob_Handler
#Spawns Friendly and Enemy npcs

import bge
cont = bge.logic.getCurrentController()
own = cont.own
scene = bge.scene.getCurrentScene()

#NPC logic and data will be bundled in logic brick with NPC's object
def add_npc(entity_name, spawn_location):
  
def remove_npc(entity_active_id_val):
  
